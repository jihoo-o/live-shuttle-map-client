export default class Users {
    // GET
    getProfile(userId) {}

    // POST
    // edit state
    postProfile(profile) {}

    // PUT
    putReady(userId) {}
    putRunning(userId) {}
    putBlocked(userId) {}

    // DELETE
}
