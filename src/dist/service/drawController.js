export class drawController {
    constructor(map) {
        this.map = map;
    }
    drawCircle(center, radius) {
        return this.map.drawCircle(center, radius);
    }
}
//# sourceMappingURL=drawController.js.map